name: Release

on:
  workflow_dispatch:
  push:
    branches:
      - main  # Only trigger workflow when PRs are merged into main

jobs:
  release:
    uses: Shattered-Codex/ci-workflows/.github/workflows/foundry-ci-release.yml@main
    secrets:
      GITHUB_TOKEN: ${{ secrets.CI_WORKFLOWS_TOKEN }}

      