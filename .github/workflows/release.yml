# .github/workflows/release.yml
# ==========================================================
# Release workflow for Foundry VTT modules (merge-based)
# Features:
#   - Detect merged branch name from PR (major/minor/patch)
#   - Automatic semantic version bump
#   - Updates module.json manifest
#   - Packages module files into ZIP
#   - Publishes GitHub Release
#   - Fully compatible with private repos in Free Plan
# ==========================================================

name: Release

on:
  workflow_dispatch:   # Allows manual run
  push:
    branches:
      - main          # Trigger only when PRs are merged into main

jobs:
  release:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    permissions:
      contents: write   # Required to create tags, commits, releases
      pull-requests: read   # Needed to read merged PRs via gh

    env:
      GH_TOKEN: ${{ github.token }}  # Default GitHub token

    steps:
      # --- STEP 1: Checkout repository ---
      - name: Checkout repository
        uses: actions/checkout@v4

      # --- STEP 2: Set MODULE_ID from repo name ---
      - name: Set MODULE_ID
        run: echo "MODULE_ID=${GITHUB_REPOSITORY##*/}" >> $GITHUB_ENV

      # --- STEP 3: Detect merged branch from PR ---
      - name: Detect merged branch from PR
        id: merged_branch
        shell: bash
        run: |
          echo "Fetching merged branch via GitHub CLI..."
          MERGED_BRANCH=$(gh pr list \
            --state merged \
            --base main \
            --limit 1 \
            --json number,headRefName \
            -q '.[0].headRefName')
          
          if [[ -z "$MERGED_BRANCH" ]]; then
            echo "Cannot detect merged branch, defaulting to patch/unknown"
            MERGED_BRANCH="patch/unknown"
          fi

          PREFIX=$(echo "$MERGED_BRANCH" | cut -d'/' -f1)
          if [[ "$PREFIX" != "major" && "$PREFIX" != "minor" && "$PREFIX" != "patch" ]]; then
            PREFIX="patch"
          fi

          echo "Merged branch: $MERGED_BRANCH"
          echo "BUMP_TYPE=$PREFIX" >> $GITHUB_ENV
          echo "MERGED_BRANCH=$MERGED_BRANCH" >> $GITHUB_ENV

      # --- STEP 4: Bump version and push tag ---
      - name: Bump semantic version
        id: tag_version
        uses: mathieudutour/github-tag-action@v6.2
        with:
          github_token: ${{ github.token }}
          default_bump: ${{ env.BUMP_TYPE }}

      # --- STEP 5: Update module.json manifest ---
      - name: Update module manifest
        uses: microsoft/variable-substitution@v1
        with:
          files: "module.json"
        env:
          version: ${{ steps.tag_version.outputs.new_version }}
          id: ${{ env.MODULE_ID }}
          url: https://github.com/${{ github.repository }}
          download: https://github.com/${{ github.repository }}/releases/download/${{ steps.tag_version.outputs.new_tag }}/${{ env.MODULE_ID }}.zip
          manifest: https://github.com/${{ github.repository }}/releases/latest/download/module.json
          flags.hotReload: false

      # --- STEP 5B: Commit and push manifest update ---
      - name: Commit module manifest update
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add module.json
          if git diff --cached --quiet; then
            echo "Manifest already up to date; nothing to commit."
            exit 0
          fi
          git commit -m "chore: sync module manifest to ${{ steps.tag_version.outputs.new_version }}"
          git push https://x-access-token:${GH_TOKEN}@github.com/${{ github.repository }} HEAD:${GITHUB_REF#refs/heads/}

      # --- STEP 6: Create ZIP archive ---
      - name: Create ZIP archive
        run: |
          zip -r ./${{ env.MODULE_ID }}.zip \
            module.json README.md LICENSE CHANGELOG.md \
            css/ img/ lang/ packs/ scripts/ templates/ assets/ || true

      # --- STEP 7: Ensure CHANGELOG.md exists ---
      - name: Ensure CHANGELOG.md exists
        run: |
          if [ ! -f CHANGELOG.md ]; then
            echo "No changelog yet." > CHANGELOG.md
          fi

      # --- STEP 8: Generate CHANGELOG.md ---
      - name: Generate CHANGELOG.md
        shell: bash
        run: |
          echo "Generating CHANGELOG.md from commits..."
          LAST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "")
          if [ -z "$LAST_TAG" ]; then
            git log --pretty=format:"* %s" > CHANGELOG.md
          else
            git log $LAST_TAG..HEAD --pretty=format:"* %s" > CHANGELOG.md
          fi

      # --- STEP 9: Publish GitHub Release ---
      - name: Create GitHub Release
        uses: ncipollo/release-action@v1
        with:
          tag: ${{ steps.tag_version.outputs.new_tag }}
          name: Release ${{ steps.tag_version.outputs.new_tag }}
          bodyFile: CHANGELOG.md
          allowUpdates: true
          artifacts: "./module.json, ./${{ env.MODULE_ID }}.zip, ./CHANGELOG.md"
          token: ${{ github.token }}

      - name: Notify Discord
        if: ${{ success() }}
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          VERSION: ${{ steps.tag_version.outputs.new_version }}
        shell: bash
        run: |
          if [ -z "${DISCORD_WEBHOOK_URL}" ]; then
            echo "DISCORD_WEBHOOK_URL is not configured. Skipping Discord notification."
            exit 0
          fi

          MODULE_TITLE=$(jq -r '.title // .id // env.MODULE_ID' module.json)
          DOWNLOAD_HUB_URL="https://app.shattered-codex.com/"
          COMMIT_MESSAGE=$(git log -1 --pretty=%B "${GITHUB_SHA}" 2>/dev/null || true)

          if [ -z "${COMMIT_MESSAGE//[$'\t\r\n ']}" ]; then
            COMMIT_MESSAGE=$(gh api \
              -H "Accept: application/vnd.github+json" \
              "/repos/${GITHUB_REPOSITORY}/commits/${GITHUB_SHA}/pulls" \
              --jq '.[0].title // empty' 2>/dev/null || true)
          fi

          if [ -z "${COMMIT_MESSAGE}" ]; then
            COMMIT_MESSAGE="Release published"
          fi

          COVER_IMAGE=""
          if [ -f "assets/img/cover.webp" ]; then
            COVER_IMAGE="assets/img/cover.webp"
          elif [ -f "assets/imgs/cover.webp" ]; then
            COVER_IMAGE="assets/imgs/cover.webp"
          fi

          if [ -n "${COVER_IMAGE}" ]; then
            EMBED_IMAGE_URL="attachment://cover.webp"
          else
            EMBED_IMAGE_URL=$(jq -r 'first(.media[]? | select(.type == "cover") | .url) // empty' module.json)
          fi

          payload=$(jq -n \
            --arg content "@everyone" \
            --arg title "${MODULE_TITLE} v${VERSION}" \
            --arg url "${DOWNLOAD_HUB_URL}" \
            --arg version "${VERSION}" \
            --arg commit "${COMMIT_MESSAGE}" \
            --arg image_url "${EMBED_IMAGE_URL}" \
            '($commit | if length > 3800 then .[0:3800] + "\n\n*… truncated by Discord limit*" else . end) as $commit_body
            | {
              content: $content,
              allowed_mentions: { parse: ["everyone"] },
              embeds: [
                {
                  title: $title,
                  url: $url,
                  description: "**A new release has been published.**\n\n**Version:** `\($version)`\n\n\($commit_body)",
                  color: 15418782
                }
              ]
            }
            | if $image_url != "" then .embeds[0].image = { url: $image_url } else . end')

          if [ -n "${COVER_IMAGE}" ]; then
            curl -sSf \
              -F "payload_json=${payload}" \
              -F "files[0]=@${COVER_IMAGE};filename=cover.webp;type=image/webp" \
              "${DISCORD_WEBHOOK_URL}"
          else
            curl -sSf \
              -H "Content-Type: application/json" \
              -d "${payload}" \
              "${DISCORD_WEBHOOK_URL}"
          fi

      # --- STEP 10: Publish release to Foundry ---
      - name: Publish to Foundry Package API
        if: ${{ success() }}
        env:
          RELEASE_TOKEN: ${{ secrets.FOUNDRY_RELEASE_TOKEN }}
          VERSION: ${{ steps.tag_version.outputs.new_version }}
          TAG: ${{ steps.tag_version.outputs.new_tag }}
        run: |
          if [ -z "${RELEASE_TOKEN}" ]; then
            echo "FOUNDRY_RELEASE_TOKEN is not configured. Skipping Foundry publish."
            exit 0
          fi

          MANIFEST_URL="https://github.com/${GITHUB_REPOSITORY}/releases/download/${TAG}/module.json"
          NOTES_URL="https://github.com/${GITHUB_REPOSITORY}/releases/tag/${TAG}"

          COMPAT_MIN=$(jq -r '.compatibility.minimum // empty' module.json)
          COMPAT_VERIFIED=$(jq -r '.compatibility.verified // empty' module.json)
          COMPAT_MAX=$(jq -r '.compatibility.maximum // empty' module.json)

          if [ -z "${COMPAT_MIN}" ] || [ -z "${COMPAT_VERIFIED}" ]; then
            echo "Compatibility data is missing from module.json. Skipping Foundry publish."
            exit 0
          fi

          payload=$(jq -n \
            --arg id "${MODULE_ID}" \
            --arg version "${VERSION}" \
            --arg manifest "${MANIFEST_URL}" \
            --arg notes "${NOTES_URL}" \
            --arg min "${COMPAT_MIN}" \
            --arg verified "${COMPAT_VERIFIED}" \
            --arg max "${COMPAT_MAX}" \
            '{id:$id, release:{version:$version, manifest:$manifest, notes:$notes, compatibility:{minimum:$min, verified:$verified, maximum:$max}}}')

          curl -sSf -X POST \
            -H "Content-Type: application/json" \
            -H "Authorization: ${RELEASE_TOKEN}" \
            -d "${payload}" \
            https://foundryvtt.com/_api/packages/release_version/
