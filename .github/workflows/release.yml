name: Release

on:
  workflow_dispatch:
  push:
    branches:
      - main  # Only trigger workflow when PRs are merged into main

jobs:
  release:
    uses: Shattered-Codex/ci-workflows/.github/workflows/foundry-ci-release.yml@v1
    secrets:
      GITHUB_TOKEN: ${{ secrets.CI_WORKFLOWS_TOKEN }}

      