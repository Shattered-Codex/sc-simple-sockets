name: Release

on:
  push:
    branches:
      - main  # Only trigger workflow when PRs are merged into main

jobs:
  release:
    uses: Shattered-Codex/ci-workflows/blob/v1/.github/workflows/foundry-ci-release.yml
    secrets:
      GITHUB_TOKEN: ${{ secrets.CI_WORKFLOWS_TOKEN }}

      